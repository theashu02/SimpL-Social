{
{
  "builds": [
    {
      "src": "src/main.js",  // Your entry file
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"  // The default Vite output directory
      }
    }
  ]
}

